 #include <iostream>
 #include <cstring>
 #include <algorithm>
 #include <cstdio>
 
 using namespace std;
 
 int n, m;
 char c[5];
 
 // lsh 
 
 int main() {
// 	cin >> n >> m;
	scanf("%d%d", &n, &m)
	for (int i = 0 ; i <= n; i++) {
		par[i] = i;
	}
	
 	for (int i = 0; i < n; i++) {
 		int l, r;
 		scanf("%d%d%s", &l, &r, s);
 		
	}
 	
 	return 0;
 }
 


