#include <bits/stdc++.h>
using namespace std;

int s, n, m;
int em[1005][1005];
int main() {
    cin >> s >> n >> m;
    for (int i = 1; i <= s; i++) {
        for (int j = 1; j <= m; j++) {
            cin >> em[i][j];
        }
    }

}